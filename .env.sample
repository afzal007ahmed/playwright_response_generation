# Base API endpoint URL to send reservation requests to
URL=http://localhost:3000

# Authentication / Bearer token (leave empty if not needed)
AUTH_TOKEN=your_auth_token_here

# Path to your test cases file (.json, .csv, or .xlsx)
FILE_PATH=./sample_testcases.json
